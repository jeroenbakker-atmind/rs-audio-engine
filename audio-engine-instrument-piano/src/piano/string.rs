#[derive(Default, Copy, Clone)]
pub struct PianoString {}

impl PianoString {
    pub fn init(
        &mut self,
        f: f32,
        fs: f32,
        inpos: f32,
        c1: f32,
        c3: f32,
        b: f32,
        z: f32,
        zb: f32,
        zh: f32,
    ) {
    }
}
